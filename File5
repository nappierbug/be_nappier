import { Code, Users, Lock, Zap } from "lucide-react";

const useCases = [
  {
    icon: Code,
    title: "Developers",
    description: "Testing scripts or tools"
  },
  {
    icon: Users,
    title: "Power Users",
    description: "Managing files and processes"
  },
  {
    icon: Lock,
    title: "Security Learners",
    description: "Practicing ethical hacking commands"
  },
  {
    icon: Zap,
    title: "Automation",
    description: "Automating backups and updates"
  }
];

export const UseCases = () => {
  return (
    <section className="px-6 py-24 relative">
      <div className="container mx-auto max-w-6xl">
        <div className="text-center mb-16">
          <h2 className="text-3xl md:text-5xl font-bold mb-4">
            <span className="text-primary terminal-glow">Use</span> Cases
          </h2>
          <p className="text-muted-foreground text-lg">
            Perfect for developers, power users, and tech enthusiasts
          </p>
        </div>
        
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {useCases.map((useCase, index) => (
            <div 
              key={index}
              className="bg-card border border-primary/20 rounded-lg p-6 text-center hover:border-primary/50 transition-all duration-300 card-glow backdrop-blur-sm"
            >
              <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary/10 mb-4">
                <useCase.icon className="w-8 h-8 text-primary" />
              </div>
              <h3 className="text-lg font-bold mb-2 text-foreground">{useCase.title}</h3>
              <p className="text-sm text-muted-foreground">{useCase.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
};
